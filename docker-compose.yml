services:
  sync-engine:
    build: .
    container_name: linear-sync-engine
    restart: unless-stopped
    user: "${UID:-1000}:${GID:-1000}"
    env_file:
      - .env
    volumes:
      - ./state:/app/state
      - ./logs:/app/logs
